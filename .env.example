# Desarrollo (npm run dev): ver .env.development — proxy /api/v1 -> localhost:1001
# Producción (npm run build): ver .env.production
VITE_API_URL=/api/v1

# UI de prueba SMTP (/mail/test). Por defecto oculta.
# VITE_MAIL_TEST_UI_ENABLED=true
